{
  "scripts": {
    "serve": "npx serve",
    "build": "pug templates/presentations --out build --basedir .",
    "copyAssets": "rsync -a --delete assets/ build/assets/",
    "package": "npm run build && npm run copyAssets"
  },
  "dependencies": {
    "pug": "^3.0.2",
    "reveal.js-internation": "^1.0.4"
  },
  "devDependencies": {
    "pug-cli": "^1.0.0-alpha6"
  }
}
